// Iterate through a given array, printing each value.

function iterateArr(arr) {
    for(let i = 0; i < arr.length; i++) {
        console.log(arr[i]);
    }
}

iterateArr([4, "hello", 35, "world", 2])